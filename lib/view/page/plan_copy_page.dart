import 'package:flutter/material.dart';

class PlanCopyPage extends StatelessWidget {
  const PlanCopyPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Center(child: Text('copy plan')),
    );
  }
}
